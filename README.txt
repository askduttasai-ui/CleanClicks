🧹 CleanClicks v3.0 — Your PC's Best Friend
============================================
Free forever. No ads. No cloud. No telemetry.
More powerful than CCleaner Professional — at zero cost.

── WHAT'S NEW IN v3.0 ──────────────────────────────────────
  🔒 Secure File Shredder  — DoD / Gutmann overwrite (paid in CCleaner)
  🛠 Registry Cleaner      — Fix broken entries, invalid startup paths
  📊 Live System Monitor   — Real-time CPU, RAM, disk + top processes
  📦 Standalone EXE        — One-click, no Python needed (run build_exe.bat)

── QUICK START (For your friend) ─────────────────────────────
  Option A — EXE (Easiest, no Python needed):
    1. Run build_exe.bat on YOUR machine first
    2. Send your friend: dist\CleanClicks.exe + cleanclicks.html
    3. They double-click CleanClicks.exe — done!

  Option B — Installer (Python needed once):
    1. Right-click install.bat -> Run as administrator
    2. Wait 2-3 minutes
    3. CleanClicks opens on Desktop automatically!

── FULL FEATURE LIST ────────────────────────────────────────
  ✅ Temp and Cache Cleaner     — Browser, Windows temp, logs
  ✅ Duplicate Finder           — SHA-256 hash, catches renamed copies
  ✅ Startup Manager            — Speed up boot time
  ✅ Large File Finder          — Find hidden space hogs
  ✅ Privacy Cleaner            — Browser history, cookies, recent files
  ✅ Auto-Clean every 5 min     — Silent background cleaning (UNIQUE)
  ✅ PC Health Score A-F        — Single grade for your PC (UNIQUE)
  ✅ Disk Space Trend Chart     — Free space over time (UNIQUE)
  ✅ Runs as Windows Service    — No app to keep open (UNIQUE)
  ✅ Persistent History         — Survives reboots (UNIQUE)
  ✅ Windows Notifications      — Toast after every clean (UNIQUE)
  🆕 Secure File Shredder       — DoD/Gutmann, permanently unrecoverable
  🆕 Registry Cleaner           — Fix broken paths and startup entries
  🆕 Live System Monitor        — CPU, RAM, disk usage, top processes

── vs COMPETITORS ───────────────────────────────────────────
  Feature                 CCleaner  Glary   CleanClicks
  Auto-Clean (free)         NO       NO        YES ★
  Windows Service           NO       NO        YES ★
  Health Score Grade        NO       NO        YES ★
  Disk Trend Chart          NO       NO        YES ★
  Secure Shredder (free)    PAID     YES       YES
  Registry Cleaner          YES      YES       YES
  Live Monitor              YES      YES       YES
  Zero ads / telemetry      NO       NO        YES ★
  Standalone EXE            YES      YES       YES

── FILES ────────────────────────────────────────────────────
  cleanclicks.html         The app UI
  cleaner_backend.py       The engine
  cleanclicks_service.py   Windows Service wrapper
  install.bat              One-click installer
  build_exe.bat            Build a .exe (no Python needed for recipient)
  service_manager.bat      Start / Stop / Restart the service
  uninstall.bat            Clean removal

CleanClicks v3.1 — © 2025 AK CleanClicks — Free forever
